// Model payloads are decoded from the core once and never mutated in place, so the read-only
// collection interfaces they expose can be treated as stable. Without this every screen taking a
// state object with a list field is compared by instance and re-runs on any unrelated change.
kotlin.collections.List
kotlin.collections.Set
kotlin.collections.Map
